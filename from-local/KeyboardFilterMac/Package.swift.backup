// swift-tools-version:5.7
import PackageDescription

let package = Package(
    name: "KeyboardFilterMac",
    platforms: [
        .macOS(.v11)
    ],
    products: [
        .executable(
            name: "KeyboardFilterMac",
            targets: ["KeyboardFilterMac"]),
    ],
    dependencies: [
    ],
    targets: [
        .executableTarget(
            name: "KeyboardFilterMac",
            dependencies: [],
            path: "Sources",
            resources: [
                .process("Resources")
            ],
            linkerSettings: [
                .linkedFramework("Cocoa"),
                .linkedFramework("CoreGraphics")
            ]
        )
    ]
)